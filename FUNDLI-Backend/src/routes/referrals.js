const express = require('express');
const router = express.Router();

// Placeholder for referral routes
router.get('/', (req, res) => {
  res.json({ message: 'Referrals route - to be implemented' });
});

module.exports = router; 