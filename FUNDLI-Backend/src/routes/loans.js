const express = require('express');
const router = express.Router();

// Placeholder for loan routes
router.get('/', (req, res) => {
  res.json({ message: 'Loans route - to be implemented' });
});

module.exports = router; 